include: package:very_good_analysis/analysis_options.yaml

analyzer:
  exclude:
    - "**/*.g.dart"
    - "**/*.freezed.dart"
  errors:
    # False positive: Riverpod providers reference these members but
    # the analyzer can't trace through code-gen providers.
    unreachable_from_main: ignore
